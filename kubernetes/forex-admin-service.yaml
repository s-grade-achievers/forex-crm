# forex-admin-service.yaml
apiVersion: v1
kind: Service
metadata:
    name: forex-admin
    namespace: forex-crm
spec:
    ports:
        - port: 3000
          targetPort: 3000
    selector:
        app: forex-admin
